/** PUBLIC_INTERFACE
 * Placeholder custom edge renderer; defer to default by returning null.
 */
export default function ExampleEdge(props) {
  /** Placeholder custom edge renderer; defer to default by returning null. */
  return null;
}
